mixin quiz5(indice)
  section.sec5.question(question=indice)
    .titleBloc.up
      .number.zoomIn 
        +image('questao05n.svg', '')
      .titleD Toda criança com HPP vai ter os mesmos sintomas.
    .blocBase
      .bloc.down.delay2
        .it(it=1, res=0 )
          .sel
          .text Verdadeiro
        .it(it=2, res=1 )
          .sel
          .text Falso
        

    .boxBlack.up.delay6
      button.btnInit.cadastro.confirm
        +image('enviarBtn.svg', '')
      
